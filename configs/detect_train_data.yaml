# dataset.yaml
# 指针式仪表目标检测数据集配置

# 数据集根目录（请修改为你的实际路径）
path: dataset/data_detection/data_detection

# 训练集和验证集路径（相对于path）
train: images/train
val: images/test

# 类别数量
nc: 1

# 类别名称（索引0对应的名称）
names:
  0: meter